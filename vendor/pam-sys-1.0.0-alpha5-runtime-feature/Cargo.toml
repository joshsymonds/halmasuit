# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2018"
name = "pam-sys"
version = "1.0.0-alpha5"
authors = ["Florian Wilkens <gh@1wilkens.org>"]
build = "build.rs"
links = "pam"
include = [
    "LICENSE-*",
    "README.md",
    "CHANGELOG.md",
    "src/lib.rs",
    "build.rs",
    "wrapper.h",
]
description = "FFI wrappers for the Linux Pluggable Authentication Modules (PAM)"
documentation = "https://docs.rs/pam-sys/"
readme = "README.md"
categories = [
    "authentication",
    "api-bindings",
    "external-ffi-bindings",
    "os::unix-apis",
]
license = "MIT OR Apache-2.0"
repository = "https://github.com/1wilkens/pam-sys.git"

[dependencies.libc]
version = "^0.2.65"

[build-dependencies.bindgen]
version = "0.69"
# Vendored override (halmasuit Epic #12): upstream pam-sys 1.0.0-alpha5
# sets `default-features = false`, disabling bindgen's `runtime` feature.
# That disables clang-sys's runtime libclang-loading path. When another
# workspace consumer (rsmpeg → rusty_ffmpeg → bindgen 0.71) enables
# `clang-sys/runtime`, cargo unifies the feature globally — but
# pam-sys's bindgen 0.69 build.rs doesn't call `clang_sys::load()`
# because IT didn't request runtime. clang-sys 1.8.1 then panics
# inside the runtime-arm `link!` macro at "a `libclang` shared library
# is not loaded on this thread" (clang-sys#128 — open since 2021).
# Enabling bindgen's default features (which includes `runtime`)
# makes pam-sys's bindgen call `clang_sys::load()` itself, agreeing
# with the unified feature set.
default-features = true

[badges.azure-devops]
pipeline = "pam-sys"
project = "1wilkens/ci"

# Vendored override note (halmasuit Epic #12): cargo-machete can't see
# that libc is used in the FFI bindings (which the build.rs generates
# at compile time from <security/pam_appl.h>). Marking it ignored so
# `just check` stays clean.
[package.metadata.cargo-machete]
ignored = ["libc"]
